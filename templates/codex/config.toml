# GSD/Pi workstation Codex defaults.
# The installer merges these values only after the pinned safety-net plugin
# is installed and its destructive-command self-test passes.

approval_policy = "never"
sandbox_mode = "danger-full-access"
sandbox = "danger-full-access"
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
service_tier = "default"

[features]
plugin_hooks = true
